spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/compute_service
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: false
  cache:
    type: simple

server:
  port: 8080

logging:
  level:
    root: INFO
    com.example.compute_service: INFO
